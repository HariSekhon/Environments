#!/usr/bin/env bash
#
# Generated using aws_profile_generate_direnvs.sh from:
#
#   https://github.com/HariSekhon/DevOps-Bash-tools

export AWS_PROFILE=prod

export EKS_CLUSTER=prod
#export EKS_NAMESPACE=prod

. ../.envrc

# ============================================================================ #
# If copying this .envrc to terraform / terragrunt directories in a different part of the repo

#git_root="$(git rev-parse --show-toplevel)"

# shellcheck disable=SC1091
#. "$git_root/aws/.envrc"

# ============================================================================ #
# If the shared .envrc is in another repo in an adjacent checkout,
# check for its checked out existence and only import it if it's available

#git_root="$(git rev-parse --show-toplevel)"

#envrc="$git_root/../environments/aws/.envrc"

#if [ -f "$envrc" ]; then
#    # shellcheck disable=SC1091
#    . "$git_root/../environments/aws/.envrc"
#fi
